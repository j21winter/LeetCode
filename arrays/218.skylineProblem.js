
function getSkyline(buildings){
    const storedY = 0
    const storedX = 0
    const prevBuilding = []

    const skyline = []

    for(building of buildings){
        // first value
        if(previousBuilding = null){
            storedX = building[0] // left most x value
            storedY = building[2] // height if the building
            previousBuilding = building
        } else { // further values
            // compare 
            if(building[0] > previousBuilding[0] ){

            }

            // add if required 
        }
    }
}